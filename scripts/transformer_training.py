## to be filled with transformer training ## 

transformer = TransformerRegressor(
     output_dim=output_dim,
     param_dim=param_dim,
     n_sources=n_sources,
     d_model=d_model,
     nhead=nhead,
     num_encoder_layers=num_encoder_layers,
     dim_feedforward=dim_feedforward,
     dropout=dropout,
)
